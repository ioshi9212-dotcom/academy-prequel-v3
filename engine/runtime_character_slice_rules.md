# Runtime Character Slice Rules

Use `runtime/characters/*.yaml` as the main source in play mode. These files must contain compact identity, scene function, speech style, behavior pressure and do/don't rules.

Full character files are fallback only. Relationship and knowledge slices override generic behavior when they conflict.
